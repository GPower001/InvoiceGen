## Packages
jspdf | PDF generation library
html2canvas | Convert DOM elements to canvas for PDF generation
date-fns | Date formatting

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
